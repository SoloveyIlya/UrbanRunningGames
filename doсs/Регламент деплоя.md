# Регламент деплоя — Urban Running Games

Конфигурации сервера (nginx, logrotate, SSL): папка **`deploy/`** — см. [deploy/README.md](../deploy/README.md)

---

## Подготовка к деплою

**Перед деплоем необходимо:**

1. **Проверить изменения:**
   - Убедиться, что все изменения закоммичены и запушены в репозиторий
   - Проверить, что тесты проходят (если есть)
   - Убедиться, что миграции БД готовы и протестированы локально

2. **Проверить окружение:**
   - Проверить наличие всех необходимых переменных окружения в `.env` на сервере:
     - `APP_ENV=production`
     - `APP_DEBUG=false`
     - `LOG_CHANNEL`, `LOG_STACK` и `LOG_LEVEL` — для production: `LOG_STACK=daily`, `LOG_DAILY_DAYS=14`
     - Настройки БД (`DB_*`)
     - Настройки почты (`MAIL_*`) для отправки уведомлений
   - Убедиться, что версии PHP (8.4), MySQL (8) соответствуют требованиям проекта
   - Проверить доступность сервисов (БД, почта, внешние API)
   - Убедиться, что права доступа к `storage/` и `bootstrap/cache/` настроены корректно
   - Для обработки изображений (галерея, превью): расширение PHP **GD** или **Imagick** (требуется для `intervention/image-laravel`)

3. **Резервное копирование:**
   - Создать бэкап базы данных: `mysqldump -u user -p database > backup_$(date +%Y%m%d_%H%M%S).sql`
   - Сохранить текущую версию кода (тег или бэкап директории)
   - Проверить наличие бэкапов медиафайлов (если используются)

## Процесс деплоя (Production)

**Шаг 1: Подключение к серверу**
```bash
ssh user@production-server
cd /var/www/urban-running-games
```

**Шаг 2: Переключение в режим обслуживания (опционально)**
```bash
php artisan down --message="Обновление системы" --retry=60
```

**Шаг 3: Получение последних изменений**
```bash
git fetch origin
git checkout master  # или main
git pull origin master
```

**Шаг 4: Установка зависимостей**
```bash
composer install --no-dev --optimize-autoloader
npm install --production
npm run build
```

**Шаг 5: Применение миграций БД**
```bash
php artisan migrate --force
```

**Примечание:** Если это первый деплой или были добавлены новые миграции, убедитесь, что все таблицы созданы корректно:
- `events`, `cities`, `partners`, `event_partners`
- `contact_messages`
- `media_assets` (в т.ч. колонка `thumbnail_path` для превью)
- `albums`, `album_items`
- **Магазин:** `products`, `product_media`, `product_variants`, `orders`, `order_items`, `promo_codes`, `promo_code_product`

**Шаг 6: Очистка кэша**
```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
php artisan optimize
```

**Шаг 7: Проверка прав доступа**
```bash
chmod -R 755 storage bootstrap/cache
chown -R www-data:www-data storage bootstrap/cache
```

**Шаг 8: Выход из режима обслуживания**
```bash
php artisan up
```

**Шаг 9: Проверка работоспособности**
- Проверить главную страницу сайта (`/`)
- Проверить навигацию и все основные разделы:
  - О команде (`/about`)
  - Правила забега (`/rules`)
  - События (`/events`)
  - Архив событий (`/archive`)
  - **Фотогалерея** (`/gallery`) — список событий с альбомами, открытие альбома и отображение фото
  - Партнёры (`/partners`)
  - Контакты (`/contact`)
  - Рейтинг (`/rating`)
  - Юридические страницы (`/privacy`, `/consent`, `/terms`, `/returns`)
- Проверить форму обратной связи (отправка сообщения)
- Проверить отображение событий (предстоящие и архивные)
- Проверить карточки событий (детальная информация, блок «Фотогалерея» со ссылками на альбомы)
- Проверить загрузку изображений по URL `/storage/...` (фото и превью в галерее). При 404 на симлинке раздача идёт через Laravel (fallback).
- Проверить админ-панель: раздел «Альбомы», массовая загрузка фото в альбом, превью в списке
- **Магазин (публично):** каталог (`/shop`), карточка товара, корзина, оформление заявки (имя, телефон, email, комментарий), промокод в корзине, экран подтверждения заявки
- **Админка магазина:** раздел «Магазин» — CRUD товаров (фото, варианты), управление заявками (статусы: создана, в обработке, подтверждена, отменена, завершена, отправлена), CRUD промокодов, статистика использования промокодов на странице промокодов, экспорт заявок в CSV (кнопка «Экспорт CSV» в списке заявок)
- Проверить логи на наличие ошибок: `tail -f storage/logs/laravel.log`

## Процесс деплоя (Staging)

Staging деплой выполняется аналогично Production, но:
- Используется отдельный vhost или поддомен (например, `stage.urban-running-games.ru`)
- Можно использовать `--dev` флаги для composer
- Можно оставить `APP_DEBUG=true` для отладки
- Миграции применяются с предупреждениями (без `--force`)

## Автоматизированный деплой через CI/CD

При **push в ветку `master`** деплой выполняется автоматически через GitHub Actions.

**Workflow:** `.github/workflows/deploy.yml`

**Настройка GitHub Secrets** (Settings → Secrets and variables → Actions):

| Secret | Описание |
|--------|----------|
| `PROD_HOST` | IP или hostname сервера |
| `PROD_USER` | SSH-пользователь |
| `PROD_SSH_KEY` | Приватный SSH-ключ (полное содержимое) |

**Путь к проекту на сервере:** `/var/www/urban-running-games` (при другом пути — отредактировать workflow).

**Выполняемые шаги:**
1. `git pull origin master`
2. `composer install --no-dev --optimize-autoloader`
3. `npm install --production` + `npm run build`
4. `php artisan storage:link`
5. `php artisan migrate --force`
6. Очистка кэша и `php artisan optimize`

## Откат (Rollback)

**Если деплой прошёл неудачно:**

1. **Быстрый откат к предыдущей версии:**
```bash
git log --oneline  # найти предыдущий коммит
git checkout <previous-commit-hash>
composer install --no-dev --optimize-autoloader
php artisan migrate:rollback  # если были миграции
php artisan config:clear
php artisan cache:clear
php artisan optimize
php artisan up
```

2. **Восстановление БД из бэкапа:**
```bash
mysql -u user -p database < backup_YYYYMMDD_HHMMSS.sql
```

3. **Проверка после отката:**
- Убедиться, что сайт работает
- Проверить логи на ошибки
- Уведомить команду о проблеме

## Чек-лист после деплоя

### Основные страницы
- [ ] Главная страница загружается корректно
- [ ] Навигация работает на всех страницах
- [ ] Мобильное меню функционирует
- [ ] Футер отображается корректно

### Публичные страницы
- [ ] Страница "О команде" доступна
- [ ] Страница "Правила забега" доступна
- [ ] Страница "Партнёры и спонсоры" отображается
- [ ] Страница "Сводный рейтинг" доступна
- [ ] Все юридические страницы доступны

### События
- [ ] Страница "События" отображает список событий
- [ ] Карточки событий показывают корректную информацию (дата, город, место)
- [ ] Статус событий определяется правильно ("Предстоящее" / "Завершено")
- [ ] Карточка события отображает полную информацию
- [ ] Партнёры события отображаются (если есть)
- [ ] На странице события отображается блок «Фотогалерея» со ссылками на альбомы (если есть опубликованные альбомы)
- [ ] Архив событий группируется по годам
- [ ] В архиве только прошедшие события

### Фотогалерея и медиа
- [ ] Страница «Фотогалерея» (`/gallery`) открывается, отображаются события с альбомами
- [ ] Альбомы открываются по ссылке, отображаются превью и фото
- [ ] Изображения по адресам `/storage/gallery/...` загружаются (основное фото и превью)
- [ ] В админке раздел «Альбомы» доступен, можно создавать альбомы и привязывать к событиям
- [ ] Массовая загрузка фото в альбом работает, превью генерируются

### Формы и функциональность
- [ ] Форма обратной связи отправляет сообщения
- [ ] Валидация формы работает корректно
- [ ] Сообщения сохраняются в БД
- [ ] Админ-панель работает (если настроена)
- [ ] Заявки на мерч создаются (каталог, корзина, оформление заявки, промокоды)

### Магазин и админка магазина
- [ ] Каталог товаров и карточка товара открываются
- [ ] Добавление в корзину, корзина, применение промокода, оформление заявки работают
- [ ] Экран подтверждения заявки отображается после отправки
- [ ] В админке: товары (CRUD), заявки (просмотр, смена статуса), промокоды (CRUD)
- [ ] Статистика использования промокодов отображается на странице «Промокоды»
- [ ] Экспорт заявок в CSV доступен и скачивает файл

### Технические проверки
- [ ] Нет критичных ошибок в логах (`storage/logs/laravel.log` или `laravel-YYYY-MM-DD.log` при daily)
- [ ] Нет ошибок в логах ошибок (`storage/logs/errors.log`)
- [ ] Производительность в норме (время загрузки страниц)
- [ ] HTTPS работает корректно
- [ ] Все статические ресурсы загружаются (CSS, JS)
- [ ] Симлинк `public/storage` существует (`php artisan storage:link`). При его отсутствии или если веб-сервер не отдаёт по нему файлы — раздача медиа идёт через Laravel (маршрут fallback и `StorageController`).

## Запуск сайта локально (Linux и Windows)

Один и тот же режим разработки доступен на **Linux/macOS** и **Windows**.

**Требования:** PHP 8.4, Composer, Node.js, npm (установлены и в PATH).

| ОС | Команда из корня проекта |
|----|---------------------------|
| **Linux / macOS** | `./scripts/start.sh` или `composer dev` |
| **Windows** | `scripts\start.bat` или `composer dev` |

Скрипты проверяют наличие PHP, Composer и Node.js и запускают сервер приложения, очередь и Vite одной командой. Остановка: `Ctrl+C`.

На Linux/macOS при первом запуске может понадобиться: `chmod +x scripts/start.sh`.

## Локальная разработка (встроенный PHP-сервер с роутером)

Для корректной раздачи файлов по `/storage/*` (фото галереи) можно запускать сервер **с роутером**:
```bash
php -S localhost:8000 -t public public/router.php
```
Роутер передаёт запросы к `/storage/...` в Laravel; без него возможны 404 на медиа. Команда `php artisan serve` (и скрипты выше) роутер не использует.

## Логи и HTTPS

- **Переменные окружения:** чек-лист для production — `deploy/ENV_CHECKLIST.md`
- **Ротация логов:** в production `LOG_STACK=daily`. Конфиг: `deploy/logrotate/laravel.conf`
- **HTTPS (Let's Encrypt):** инструкция — `deploy/ssl-letsencrypt.md`, nginx — `deploy/nginx/urban-running.conf`

## Мониторинг после деплоя

**Первые 30 минут после деплоя:**
- Мониторить логи: `tail -f storage/logs/laravel.log` и `tail -f storage/logs/errors.log`
- Проверять метрики сервера (CPU, память, диск)
- Отслеживать ошибки в браузере (консоль разработчика)
- Проверить работу формы обратной связи (отправить тестовое сообщение)
- Проверить отображение событий на главной странице и странице событий
- Проверить корректность работы архива событий
- Проверить загрузку фото на странице фотогалереи и в альбомах

**В течение 24 часов:**
- Проверять логи ежечасно
- Отслеживать пользовательские обращения
- Мониторить производительность БД

## Уведомления

После успешного деплоя уведомить:
- Команду разработки (в канале Битрикс)
- Клиента (если были значительные изменения)

При проблемах:
- Немедленно уведомить команду
- Задокументировать проблему
- Начать процесс отката при необходимости
